box: debian
build:
  steps:
    - arjen/hugo-build@2.9.1:
        version: "0.57.2"
        theme: hermit
        flags: --buildDrafts=true
        force_install: true
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - pahnev/gh-pages@0.3.1:
        token: $GITHUB_TOKEN
        basedir: public
        branch: master
